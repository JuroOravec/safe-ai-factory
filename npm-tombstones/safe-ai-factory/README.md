# safe-ai-factory (tombstone)

This package **does not** ship the SaifCTL CLI. It exists only to hold the npm name and point users to the real package.

**Install:**

```bash
npm install -g @safe-ai-factory/saifctl
```

Then run `saifctl --help`.
