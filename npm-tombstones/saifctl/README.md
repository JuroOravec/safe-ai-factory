# saifctl (tombstone)

The **official** SaifCTL CLI is published under the org scope:

```bash
npm install -g @safe-ai-factory/saifctl
```

The `saifctl` binary is provided by that package. This unscoped package exists only to prevent namespace squatting.
